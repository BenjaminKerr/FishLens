<Page x:Class="FishLens_App.History"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="History"
      Background="{DynamicResource PageBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ═══════════════════════════════════════════════════════════
             Header — PrimaryColorBrush (teal) background in both themes.
             Subtitle switched from PrimaryTextBrush (#0D3640 in light —
             invisible on teal) to WhiteBrush with reduced opacity.
        ════════════════════════════════════════════════════════════════ -->
        <Border Grid.Row="0"
                Background="{DynamicResource HistoryHeaderBackgroundBrush}"
                Padding="30,25">
            <StackPanel>
                <TextBlock Text="Reports &amp; Analytics"
                           FontSize="{DynamicResource LargeHeaderFontSize}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource WhiteBrush}"
                           Margin="0,0,0,5"/>
                <TextBlock Text="Filter, analyze, and export your detection data"
                           FontSize="{DynamicResource BaseFontSize}"
                           Foreground="{DynamicResource WhiteBrush}"
                           Opacity="0.75"
                           FontWeight="Normal"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ═══════════════════════════════════════════════════════
                 Filter sidebar
            ════════════════════════════════════════════════════════════ -->
            <Border Grid.Column="0"
                    Background="{DynamicResource SecondaryBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderColorBrush}"
                    BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ScrollViewer Grid.Row="0"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="30,20"
                                  PreviewMouseWheel="FilterPanel_PreviewMouseWheel">
                    <StackPanel>

                        <!-- "Filters" heading — PrimaryText reads in both themes -->
                        <TextBlock Text="Filters"
                                   FontSize="{DynamicResource LargeFontSize}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,15"/>

                        <!-- ── Run ────────────────────────────────────────── -->
                        <TextBlock Text="Run"
                                   FontSize="{DynamicResource BaseFontSize}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                   Margin="0,0,0,8"/>
                        <ComboBox x:Name="runFilter"
                                  Background="{DynamicResource PageBackgroundBrush}"
                                  BorderBrush="{DynamicResource BorderColorBrush}"
                                  Padding="12,8"
                                  FontSize="{DynamicResource BaseFontSize}"
                                  Margin="0,0,0,15"
                                  SelectedIndex="0"
                                  SelectionChanged="RunFilter_SelectionChanged"/>

                        <!-- ── Date Range ─────────────────────────────────── -->
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Date Range"
                                       FontSize="{DynamicResource BaseFontSize}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryText}"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"/>

                            <Button Content="Reset Dates" 
                                    Click="AllDatesClick"
                                    Background="{DynamicResource CardBackground}"
                                    Foreground="{DynamicResource PrimaryText}"
                                    BorderBrush="{DynamicResource BorderColorBrush}"
                                    BorderThickness="1"
                                    FontSize="{DynamicResource BaseFontSize}"
                                    FontWeight="SemiBold"
                                    Padding="10,6"
                                    Cursor="Hand"
                                    HorizontalAlignment="Right">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="6"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource BorderColorBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- "From" / "To" labels so the order (past → recent) is obvious -->
                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="From"
                                       FontSize="11"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       Margin="0,0,0,4"/>
                            <TextBlock Grid.Row="0" Grid.Column="2"
                                       Text="To"
                                       FontSize="11"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       Margin="0,0,0,4"/>

                            <DatePicker x:Name="startDatePicker"
                                        Grid.Row="1" Grid.Column="0"
                                        Background="{DynamicResource PageBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderColorBrush}"
                                        Padding="10,8"
                                        FontSize="{DynamicResource BaseFontSize}"/>

                            <TextBlock Grid.Row="1" Grid.Column="1"
                                       Text="—"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Foreground="{DynamicResource SecondaryText}"/>

                            <DatePicker x:Name="endDatePicker"
                                        Grid.Row="1" Grid.Column="2"
                                        Background="{DynamicResource PageBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderColorBrush}"
                                        Padding="10,8"
                                        FontSize="{DynamicResource BaseFontSize}"/>
                        </Grid>

                        <!-- ── Location ───────────────────────────────────── -->
                        <TextBlock Text="Location" 
                                   FontSize="{DynamicResource BaseFontSize}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,8"/>
                        <ComboBox x:Name="cameraFilter"
                                  Background="{DynamicResource PageBackgroundBrush}"
                                  BorderBrush="{DynamicResource BorderColorBrush}"
                                  Padding="12,8"
                                  FontSize="{DynamicResource BaseFontSize}"
                                  Margin="0,0,0,15"
                                  SelectedIndex="0"/>

                        <!-- ── Direction ──────────────────────────────────── -->
                        <TextBlock Text="Direction" 
                                   FontSize="{DynamicResource BaseFontSize}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,8"/>
                        <ComboBox x:Name="directionFilter"
                                  Background="{DynamicResource PageBackgroundBrush}"
                                  BorderBrush="{DynamicResource BorderColorBrush}"
                                  Padding="12,8"
                                  FontSize="{DynamicResource BaseFontSize}"
                                  Margin="0,0,0,15"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="All Directions"/>
                            <ComboBoxItem Content="Upstream"/>
                            <ComboBoxItem Content="Downstream"/>
                            <ComboBoxItem Content="Indecisive"/>
                        </ComboBox>

                        <!-- ── Species ────────────────────────────────────── -->
                        <TextBlock Text="Species" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,8"/>
                        <ComboBox x:Name="speciesFilter"
                                  Background="{DynamicResource PageBackgroundBrush}"
                                  BorderBrush="{DynamicResource BorderColorBrush}"
                                  Padding="12,8"
                                  FontSize="{DynamicResource BaseFontSize}"
                                  Margin="0,0,0,15"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="All"/>
                            <ComboBoxItem Content="Chinook"/>
                            <ComboBoxItem Content="Omykiss"/>
                            <ComboBoxItem Content="Other"/>
                        </ComboBox>

                        <!-- ── Confidence ─────────────────────────────────── -->
                        <TextBlock Text="Minimum Confidence" 
                                   FontSize="{DynamicResource BaseFontSize}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Slider x:Name="confidenceSlider"
                                    Grid.Column="0"
                                    Minimum="0"
                                    Maximum="100"
                                    Value="0"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    SmallChange="1"
                                    LargeChange="5"
                                    ValueChanged="ConfidenceSlider_ValueChanged"/>

                            <TextBox x:Name="confidenceValueBox"
                                     Grid.Column="1"
                                     Text="0"
                                     Width="56"
                                     Padding="8,4"
                                     FontSize="{DynamicResource BaseFontSize}"
                                     FontWeight="SemiBold"
                                     Foreground="{DynamicResource PrimaryColorBrush}"
                                     Background="{DynamicResource PageBackgroundBrush}"
                                     BorderBrush="{DynamicResource BorderColorBrush}"
                                     HorizontalContentAlignment="Center"
                                     VerticalContentAlignment="Center"
                                     Margin="10,0,0,0"
                                     VerticalAlignment="Center"
                                     LostFocus="ConfidenceValueBox_LostFocus"
                                     KeyDown="ConfidenceValueBox_KeyDown"/>
                        </Grid>

                        <TextBlock Text="Show detections at or above this confidence level"
                                   FontSize="{DynamicResource BaseFontSize}"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,0,20"/>

                        <!-- ── Action Buttons ─────────────────────────────── -->
                        <Button Content="Apply Filters" 
                                Click="ApplyFiltersClick"
                                Background="{DynamicResource AccentBrush}"
                                Foreground="{DynamicResource OnAccentForeground}"
                                BorderBrush="{DynamicResource AccentBrush}"
                                BorderThickness="1"
                                FontSize="{DynamicResource BaseFontSize}"
                                FontWeight="SemiBold"
                                Padding="0,10"
                                Cursor="Hand"
                                Margin="0,0,0,8">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource HoverBackgroundBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- ── Clear Filters — ghost button ── -->
                        <Button Content="Clear Filters"
                                Click="ClearFiltersClick"
                                Background="{DynamicResource CardBackground}"
                                Foreground="{DynamicResource PrimaryText}"
                                BorderBrush="{DynamicResource BorderColorBrush}"
                                BorderThickness="1"
                                FontSize="{DynamicResource BaseFontSize}"
                                FontWeight="SemiBold"
                                Padding="0,10"
                                Cursor="Hand"
                                Margin="0,0,0,10">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BorderColorBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button x:Name="generateReportButton"
                                Content="Generate Report" 
                                Click="GenerateReportClick"
                                Visibility="Collapsed"
                                Background="{DynamicResource PrimaryColorBrush}"
                                Foreground="{DynamicResource OnAccentForeground}"
                                FontSize="{DynamicResource BaseFontSize}"
                                FontWeight="SemiBold"
                                Padding="0,10"
                                BorderThickness="0"
                                Cursor="Hand"
                                Margin="0,0,0,12">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="6"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Border x:Name="historyAnalysisBanner"
                                Background="{DynamicResource ReportWarmSurfaceBrush}"
                                BorderBrush="{DynamicResource ReportWarmBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,8"
                                Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#x26A0;" FontSize="14" Foreground="{DynamicResource ReportWarmTitleBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Analysis in progress - report data may be incomplete."
                                           FontSize="12"
                                           Foreground="{DynamicResource ReportWarmTitleBrush}"
                                           VerticalAlignment="Center"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    </ScrollViewer>

                    <Border Grid.Row="1"
                            Background="{DynamicResource SecondaryBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderColorBrush}"
                            BorderThickness="0,1,0,0"
                            Padding="30,14">
                        <Button x:Name="printReportButton"
                                Content="Print Report"
                                Click="PrintReportClick"
                                FontSize="{DynamicResource BaseFontSize}"
                                FontWeight="SemiBold"
                                Padding="0,10"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource OnAccentForeground}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource HoverBackgroundBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Background" Value="{DynamicResource ReportPrintButtonDisabledBackground}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource ReportPrintButtonDisabledForeground}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource ReportPrintButtonDisabledBorder}"/>
                                            <Setter Property="Cursor" Value="Arrow"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Border>
                </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════════════════
                 Report card (right column)
                 Card header: switched from PageBackgroundBrush to
                 SecondaryBackgroundBrush so it doesn't punch a dark hole
                 in the card on the dark theme.
                 viewTitle: switched from PrimaryColorBrush to PrimaryText
                 so it reads in both themes.
            ════════════════════════════════════════════════════════════ -->
            <Border Grid.Column="1"
                    Margin="30,20,30,30"
                    Background="{DynamicResource SecondaryBackgroundBrush}"
                    CornerRadius="12"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource BorderColorBrush}"
                    VerticalAlignment="Stretch">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.15"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Card top bar — matches card so there's no inset contrast clash -->
                    <!-- Placeholder — emoji Foreground has no effect on glyphs, so
                         opacity sits on the TextBlock itself, not a Foreground color -->
                    <StackPanel x:Name="placeholderPanel"
                                Grid.Row="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <TextBlock Text="📊"
                                   FontSize="{DynamicResource LargeTitleFontSize}"
                                   HorizontalAlignment="Center"
                                   Opacity="0.3"/>
                        <TextBlock Text="Generate a report"
                                   FontSize="{DynamicResource LargeFontSize}"
                                   Foreground="{DynamicResource SecondaryText}"
                                   HorizontalAlignment="Center"
                                   Margin="0,20,0,0"/>
                        <TextBlock Text="Configure your filters or choose a report type" 
                                   FontSize="{DynamicResource BaseFontSize}"
                                   Foreground="{DynamicResource SecondaryText}"
                                   HorizontalAlignment="Center"
                                   Margin="0,5,0,0"/>
                    </StackPanel>

                    <MediaElement x:Name="videoPlayer"
                                  Grid.Row="0"
                                  LoadedBehavior="Manual"
                                  Stretch="Uniform"
                                  Margin="20"
                                  Visibility="Collapsed"/>

                    <ScrollViewer x:Name="reportScrollViewer"
                                  Grid.Row="0"
                                  VerticalScrollBarVisibility="Auto"
                                  Visibility="Collapsed"
                                  Padding="30,20"
                                  PreviewMouseWheel="ReportPanel_PreviewMouseWheel">
                        <StackPanel x:Name="reportPanel">
                        </StackPanel>
                    </ScrollViewer>

                </Grid>
            </Border>

        </Grid>
        <Border Grid.Row="1" Visibility="Collapsed"
                Background="{DynamicResource SecondaryBackgroundBrush}" 
                BorderBrush="{DynamicResource BorderColorBrush}" 
                BorderThickness="0,1,0,0"
                Padding="30,20">
            <Grid>
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button
                            Content="Generate Report" 
                            Click="GenerateReportClick"
                            Background="{DynamicResource PrimaryColorBrush}"
                            Foreground="{DynamicResource OnAccentForeground}"
                            FontSize="{DynamicResource BaseFontSize}"
                            FontWeight="SemiBold"
                            Padding="25,12"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Shown while analysis is running -->
                <Border
                        Background="{DynamicResource ReportWarmSurfaceBrush}"
                        BorderBrush="{DynamicResource ReportWarmBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12,8"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x26A0;" FontSize="14" Foreground="{DynamicResource ReportWarmTitleBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Analysis in progress — report data may be incomplete."
                                   FontSize="12" Foreground="{DynamicResource ReportWarmTitleBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>
