<Application x:Class="FishLens_App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FishLens_App"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             StartupUri="Pages/AuthWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Merge the default theme and video button styles so XAML can resolve theme resources -->

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/NormalTheme.xaml"/>
                <ResourceDictionary Source="Themes/VideoButtonStyles.xaml"/>
                <ResourceDictionary Source="Themes/Controls/ComboBoxStyles.xaml"/>
                <ResourceDictionary Source="Themes/Controls/ScrollBarStyles.xaml"/>
                <ResourceDictionary Source="Themes/Icons/IconAssets.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Base font sizes -->
            <system:Double x:Key="BaseFontSize">14</system:Double>
            <system:Double x:Key="LargeFontSize">18</system:Double>
            <system:Double x:Key="HeaderFontSize">24</system:Double>
            <system:Double x:Key="LargeHeaderFontSize">32</system:Double>
            <system:Double x:Key="TitleFontSize">56</system:Double>
            <system:Double x:Key="LargeTitleFontSize">72</system:Double>

            <!-- Default TextBlock Style -->
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
            </Style>

            <!-- Default Button Style -->
            <Style TargetType="Button">
                <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
            </Style>

            <!-- Accent action button (e.g. Delete Selected) – teal active, darker teal disabled -->
            <Style x:Key="AccentActionButton" TargetType="Button">
                <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="{DynamicResource HeaderBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource OnAccentForeground}"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentButtonDisabledBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource AccentButtonDisabledForeground}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Neutral action button (e.g. Change Location, Undo) – white/card active, grey disabled -->
            <Style x:Key="NeutralActionButton" TargetType="Button">
                <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource OnAccentForeground}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource NeutralButtonDisabledBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource NeutralButtonDisabledForeground}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Default TextBox Style -->
            <Style TargetType="TextBox">
                <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
