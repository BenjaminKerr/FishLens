<Page x:Class="FishLens_App.Settings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Settings"
      Background="{DynamicResource WindowBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{DynamicResource HeaderBackground}" 
                Padding="30,20">
            <TextBlock Text="Settings" 
                       FontSize="{DynamicResource LargeHeaderFontSize}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource OnAccentForeground}"/>
        </Border>

        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="30">
            <StackPanel MaxWidth="800" HorizontalAlignment="Center">

                <TextBlock Text="Analysis" 
                           FontSize="{DynamicResource LargeHeaderFontSize}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryText}"
                           Margin="0,0,0,15"/>

                <Border Background="{DynamicResource CardBackground}" 
                        BorderBrush="{DynamicResource BorderBrush}" 
                        BorderThickness="1" 
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,30">
                    <StackPanel>
                        <TextBlock Text="Confidence Threshold for Data Entry" 
                                   FontSize="{DynamicResource BaseFontSize}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,8"/>

                        <TextBlock Text="Minimum confidence level required to automatically populate analysis fields" 
                                   FontSize="{DynamicResource BaseFontSize}"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,0,10"
                                   TextWrapping="Wrap"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Slider x:Name="confidenceThreshold"
                                    Grid.Column="0"
                                    Minimum="0"
                                    Maximum="100"
                                    Value="75"
                                    TickFrequency="5"
                                    IsSnapToTickEnabled="True"
                                    IsMoveToPointEnabled="True"
                                    VerticalAlignment="Center"
                                    Margin="0,0,15,0"
                                    ValueChanged="ConfidenceThreshold_ValueChanged"
                                    PreviewMouseLeftButtonDown="ConfidenceThreshold_PreviewMouseLeftButtonDown"/>

                            <Border Grid.Column="1"
                                    Background="{DynamicResource HeaderBackground}"
                                    CornerRadius="4"
                                    Padding="12,6"
                                    MinWidth="60">
                                <TextBlock x:Name="confidenceValue" 
                                           Text="75%"
                                           FontSize="{DynamicResource BaseFontSize}"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource OnAccentForeground}"
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <TextBlock Text="Output" 
                           FontSize="{DynamicResource LargeHeaderFontSize}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryText}"
                           Margin="0,0,0,15"/>

                <StackPanel>
                    <CheckBox x:Name="hideOutput"
                              Content="Redirect Output Messages"
                              FontSize="{DynamicResource BaseFontSize}"
                              Foreground="{DynamicResource PrimaryText}"
                              Margin="0,0,0,0"
                              IsChecked="False"
                              Click="ToggleOutputMessages"/>

                    <TextBlock Text="Show output messages as a pop-up window at the end of the program instead of in the terminal."
                               FontSize="{DynamicResource BaseFontSize}"
                               Foreground="{DynamicResource SecondaryText}"
                               Margin="0,0,0,15"
                               TextWrapping="Wrap"/>

                    <CheckBox x:Name="hideErrors"
                              Content="Redirect Error Messages"
                              FontSize="{DynamicResource BaseFontSize}"
                              Foreground="{DynamicResource PrimaryText}"
                              Margin="0,0,0,0"
                              IsChecked="False"
                              Click="ToggleErrorMessages"/>

                    <TextBlock Text="Show error messages as a pop-up window at the end of the program instead of in the terminal."
                               FontSize="{DynamicResource BaseFontSize}"
                               Foreground="{DynamicResource SecondaryText}"
                               Margin="0,0,0,15"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <TextBlock Text="Accessibility" 
                           FontSize="{DynamicResource LargeHeaderFontSize}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryText}"
                           Margin="0,0,0,15"/>

                <Border Background="{DynamicResource CardBackground}" 
                        BorderBrush="{DynamicResource BorderBrush}" 
                        BorderThickness="1" 
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,30">
                    <StackPanel>
                        <CheckBox x:Name="highContrastMode"
                                  Content="High contrast mode"
                                  FontSize="{DynamicResource BaseFontSize}"
                                  Foreground="{DynamicResource PrimaryText}"
                                  Margin="0,0,0,15"/>

                        <CheckBox x:Name="largeText"
                                  Content="Use larger text throughout the application"
                                  FontSize="{DynamicResource BaseFontSize}"
                                  Foreground="{DynamicResource PrimaryText}"
                                  Margin="0,0,0,15"/>
                    </StackPanel>
                </Border>
                <TextBlock x:Name="CreateUserText" Text="Create User"
           FontSize="{DynamicResource LargeHeaderFontSize}"
           FontWeight="SemiBold"
           Foreground="{DynamicResource PrimaryText}"
           Margin="0,0,0,15"/>

                <Border x:Name="CreateUserCard" Background="{DynamicResource CardBackground}" 
        BorderBrush="{DynamicResource BorderBrush}" 
        BorderThickness="1" 
        CornerRadius="8"
        Padding="20"
        Margin="0,0,0,30">
                    <StackPanel>

                        <TextBlock Text="Add a new user account"
                   FontSize="{DynamicResource BaseFontSize}"
                   Foreground="{DynamicResource SecondaryText}"
                   Margin="0,0,0,15"
                   TextWrapping="Wrap"/>

                        <TextBlock Text="Username"
                   FontSize="{DynamicResource BaseFontSize}"
                   Foreground="{DynamicResource SecondaryText}"/>

                        <TextBox x:Name="NewUsername"
                 Height="36"
                 Padding="8"
                 FontSize="{DynamicResource BaseFontSize}"
                 Margin="0,5,0,15"/>
                        <TextBlock Text="Email"
           FontSize="{DynamicResource BaseFontSize}"
           Foreground="{DynamicResource SecondaryText}"/>

                        <TextBox x:Name="NewUserEmail"
         Height="36"
         Padding="8"
         FontSize="{DynamicResource BaseFontSize}"
         Margin="0,5,0,15"/>


                        <TextBlock Text="Password"
                   FontSize="{DynamicResource BaseFontSize}"
                   Foreground="{DynamicResource SecondaryText}"/>

                        <PasswordBox x:Name="NewPassword"
                     Height="36"
                     Padding="8"
                     FontSize="{DynamicResource BaseFontSize}"
                     Margin="0,5,0,20"/>
                        <TextBlock Text="Role"  
           FontSize="{DynamicResource BaseFontSize}"
           Foreground="{DynamicResource SecondaryText}"/>

                        <ComboBox x:Name="RoleComboBox"
          Width="200"
          HorizontalAlignment="Left"
          Margin="0,5,0,0"
          DisplayMemberPath="Name"
          SelectedValuePath="Id"
          FontSize="{DynamicResource BaseFontSize}">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Padding" Value="10,8"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Grid>
                                                    <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="32"/>
                                                            </Grid.ColumnDefinitions>

                                                            <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"/>

                                                            <!-- Down arrow -->
                                                            <Path Grid.Column="1"
                                          Data="M0,0 L4,4 L8,0"
                                          Stroke="{DynamicResource SecondaryText}"
                                          StrokeThickness="1.5"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>

                                                            <ToggleButton Grid.ColumnSpan="2"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Focusable="False"
                                                  ClickMode="Press">
                                                                <ToggleButton.Template>
                                                                    <ControlTemplate TargetType="ToggleButton">
                                                                        <Border Background="Transparent"/>
                                                                    </ControlTemplate>
                                                                </ToggleButton.Template>
                                                            </ToggleButton>
                                                        </Grid>
                                                    </Border>

                                                    <!-- Dropdown popup -->
                                                    <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                                        <Border Background="{DynamicResource CardBackground}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,4,0,0"
                                        Padding="4"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="200">
                                                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                                <StackPanel IsItemsHost="True"/>
                                                            </ScrollViewer>
                                                        </Border>
                                                    </Popup>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource HeaderBackground}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDropDownOpen" Value="True">
                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource HeaderBackground}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Style>

                            <!-- Style each dropdown item -->
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Padding" Value="10,8"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                    <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBoxItem">
                                                <Border x:Name="ItemBorder"
                                Background="Transparent"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsHighlighted" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource WindowBackground}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HeaderBackground}"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource OnAccentForeground}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>


                        <Button Content="Create User"
                Width="140"
                Padding="10,6"
                Background="{DynamicResource HeaderBackground}"
                Foreground="{DynamicResource OnAccentForeground}"
                FontSize="{DynamicResource BaseFontSize}"
                FontWeight="SemiBold"
                BorderThickness="0"
                HorizontalAlignment="Left"
                Cursor="Hand"
                Click="CreateUserClick">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6"
                                                        Margin= "0, 30 ,0, 0"
                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="0.85"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                    </StackPanel>
                </Border>
                <TextBlock x:Name="ManageUsersText" Text="Manage Users"
           FontSize="{DynamicResource LargeHeaderFontSize}"
           FontWeight="SemiBold"
           Foreground="{DynamicResource PrimaryText}"
           Margin="0,0,0,15"/>

                <Border x:Name="ManageUsersCard" Background="{DynamicResource CardBackground}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        CornerRadius="8"
        Padding="20"
        Margin="0,0,0,30">

                    <StackPanel>

                        <TextBlock Text="Edit username or role for existing accounts, then click Save Changes."
                   FontSize="{DynamicResource BaseFontSize}"
                   Foreground="{DynamicResource SecondaryText}"
                   Margin="0,0,0,15"
                   TextWrapping="Wrap"/>

                        <DataGrid x:Name="UsersGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  RowHeaderWidth="0"
                  MinHeight="240"
                  Margin="0,0,0,15"
                  BorderThickness="0"
                  Background="Transparent"
                  RowBackground="Transparent"
                  AlternatingRowBackground="Transparent"
                  SelectionUnit="FullRow"
                  FontSize="{DynamicResource BaseFontSize}" SelectionChanged="UsersGrid_SelectionChanged">

                            <!-- Modern style overrides -->
                            <DataGrid.Resources>
                                <!-- Remove the default cell focus border -->
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="12,10"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HeaderBackground}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource OnAccentForeground}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>

                                <!-- Modern column headers -->
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                </Style>

                                <!-- Modern row style with hover -->
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource WindowBackground}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource WindowBackground}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>

                                <!-- ID column completely hidden -->
                                <DataGridTextColumn Header="Id"
                        Binding="{Binding Id}"
                        IsReadOnly="True"
                        Visibility="Collapsed"/>

                                <!-- Username with styled editing -->
                                <DataGridTemplateColumn Header="Username" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Username}"
                           VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#888888"
                        BorderThickness="1"
                        CornerRadius="6"
                        Background="{DynamicResource CardBackground}">
                                                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{DynamicResource PrimaryText}"
                             FontSize="{DynamicResource BaseFontSize}"
                             Padding="10,8"
                             VerticalContentAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <!-- Email -->
                                <DataGridTemplateColumn Header="Email" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Email}"
                       VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#888888"
                    BorderThickness="1"
                    CornerRadius="6"
                    Background="{DynamicResource CardBackground}">
                                                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource PrimaryText}"
                         FontSize="{DynamicResource BaseFontSize}"
                         Padding="10,8"
                         VerticalContentAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Role with styled ComboBox -->
                                <DataGridTemplateColumn Header="Role" Width="220">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding role.Name}"
                           VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.Roles, RelativeSource={RelativeSource AncestorType=Page}}"
                          DisplayMemberPath="Name"

                          SelectedValue="{Binding role, UpdateSourceTrigger=PropertyChanged}"
                          FontSize="{DynamicResource BaseFontSize}">
                                                <ComboBox.Style>
                                                    <Style TargetType="ComboBox">
                                                        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                                        <Setter Property="BorderBrush" Value="#888888"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                        <Setter Property="Padding" Value="10,8"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ComboBox">
                                                                    <Grid>
                                                                        <Border x:Name="Border"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6">
                                                                            <Grid>
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                    <ColumnDefinition Width="32"/>
                                                                                </Grid.ColumnDefinitions>

                                                                                <ContentPresenter Grid.Column="0"
                                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                      Margin="{TemplateBinding Padding}"
                                                                      VerticalAlignment="Center"
                                                                      HorizontalAlignment="Left"/>

                                                                                <Path Grid.Column="1"
                                                          Data="M0,0 L4,4 L8,0"
                                                          Stroke="{DynamicResource SecondaryText}"
                                                          StrokeThickness="1.5"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>

                                                                                <ToggleButton Grid.ColumnSpan="2"
                                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                  Background="Transparent"
                                                                  BorderThickness="0"
                                                                  Focusable="False"
                                                                  ClickMode="Press">
                                                                                    <ToggleButton.Template>
                                                                                        <ControlTemplate TargetType="ToggleButton">
                                                                                            <Border Background="Transparent"/>
                                                                                        </ControlTemplate>
                                                                                    </ToggleButton.Template>
                                                                                </ToggleButton>
                                                                            </Grid>
                                                                        </Border>

                                                                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                                   Placement="Bottom"
                                                   AllowsTransparency="True"
                                                   Focusable="False"
                                                   PopupAnimation="Slide">
                                                                            <Border Background="{DynamicResource CardBackground}"
                                                        BorderBrush="#888888"
                                                        BorderThickness="1"
                                                        CornerRadius="6"
                                                        Margin="0,4,0,0"
                                                        Padding="4"
                                                        MinWidth="{TemplateBinding ActualWidth}"
                                                        MaxHeight="200">
                                                                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                                                    <StackPanel IsItemsHost="True"/>
                                                                                </ScrollViewer>
                                                                            </Border>
                                                                        </Popup>
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource HeaderBackground}"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsDropDownOpen" Value="True">
                                                                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource HeaderBackground}"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ComboBox.Style>

                                                <ComboBox.ItemContainerStyle>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Padding" Value="10,8"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                                        <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ComboBoxItem">
                                                                    <Border x:Name="ItemBorder"
                                                Background="Transparent"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsHighlighted" Value="True">
                                                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource WindowBackground}"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsSelected" Value="True">
                                                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HeaderBackground}"/>
                                                                            <Setter Property="Foreground" Value="{DynamicResource OnAccentForeground}"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ComboBox.ItemContainerStyle>
                                            </ComboBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                            </DataGrid.Columns>



                        </DataGrid>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Refresh"
                    Width="120"
                    Padding="10,6"
                    Background="{DynamicResource HeaderBackground}"
                    Foreground="{DynamicResource OnAccentForeground}"
                    FontSize="{DynamicResource BaseFontSize}"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="RefreshUsers_Click"
                    Margin="0,0,10,0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="0.85"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Button Content="Save Changes"
                    Width="150"
                    Padding="10,6"
                    Background="{DynamicResource HeaderBackground}"
                    Foreground="{DynamicResource OnAccentForeground}"
                    FontSize="{DynamicResource BaseFontSize}"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="SaveUserChanges_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="0.85"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>

                    </StackPanel>
                </Border>



            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" 
                Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,1,0,0"
                Padding="30,20">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">

                        <Button Content="Save Settings" 
                        Click="SaveSettings_Click"
                        Background="{DynamicResource HeaderBackground}"
                        Foreground="{DynamicResource OnAccentForeground}"
                        FontSize="{DynamicResource BaseFontSize}"
                        FontWeight="SemiBold"
                        Padding="20,10"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>